# One Operator Station First

The first live-integrated SignalDeck will assume one operator station instead of synchronized multi-client control. This avoids adding auth, persistence, and realtime server-owned state before the PCO Services integration and local Service Position workflow prove useful.
