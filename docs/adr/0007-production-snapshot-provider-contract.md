# Production Snapshot Provider Contract

SignalDeck providers will initially expose one coarse Production Snapshot rather than per-panel subscriptions, an event stream, or an external client store. The app is currently small, and one snapshot makes simulation and live providers easier to compare while the integration boundaries are still being established.
