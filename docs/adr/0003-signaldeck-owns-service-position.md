# SignalDeck Owns Service Position

PCO Services owns the planned Service Order, but SignalDeck owns the live Service Position. Live services can drift from planned durations and sequence, so the production operator needs a local execution pointer instead of relying on PCO timing, ProPresenter slide state, or automatic duration-based advancement.
