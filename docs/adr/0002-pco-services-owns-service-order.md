# PCO Services Owns Service Order

PCO Services is the canonical source of truth for the Service Order. SignalDeck should read service flow from PCO Services rather than derive it from ProPresenter or maintain an independent manual order, because the Service Order drives timing, active item context, playlist progression, and planning alignment across the production view.
