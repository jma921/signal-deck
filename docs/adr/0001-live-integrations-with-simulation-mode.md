# Live Integrations With Simulation Mode

SignalDeck will develop toward a live-integrated production tool rather than remain only a simulated dashboard prototype. Simulation mode stays as a fallback/demo fixture so the dashboard can still be developed, tested, and shown without access to church production systems.
